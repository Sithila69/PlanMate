<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.PlanMate" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item> <!-- or any dark color -->
        <item name="android:windowLightStatusBar">false</item> <!-- Dark icons for dark mode -->
        <item name="android:windowBackground">@color/black</item>
    </style>

    <style name="Theme.PlanMate" parent="Base.Theme.PlanMate" />
</resources>
